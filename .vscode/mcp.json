{
  "servers": {
    "weather-mcp-server": {
      "type": "stdio",
      "command": "node",
      "args": ["${workspaceFolder}/index.js",
        "envPath=${workspaceFolder}/.env"
      ]
    }
  }
}
